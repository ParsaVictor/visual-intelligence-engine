# Everything needed to lint and run the regression suite.
# Deliberately excludes torch: the core logic has no framework dependency, so
# the tests install and run in seconds on any machine.

numpy==2.1.3
pyyaml==6.0.2
pytest==8.3.4
ruff==0.8.4
